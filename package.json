{
  "name": "plataforma-campanas",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev:web\" \"npm run dev:api\"",
    "dev:public": "concurrently -n web,api,tunnel -c blue,green,magenta \"npm run dev:web\" \"npm run dev:api\" \"npm run tunnel\"",
    "dev:web": "npm run dev --workspace=web",
    "dev:api": "python services/python/run.py",
    "tunnel": "ngrok http 3000 --log stdout",
    "build:web": "npm run build --workspace=web",
    "lint:web": "npm run lint --workspace=web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "lightningcss-linux-x64-gnu": "^1.33.0"
  }
}
